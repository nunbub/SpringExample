<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nunbub.spring.ex.mybatis.dao.ReviewDAO">
	
	<select id="selectReview" resultType="com.nunbub.spring.ex.mybatis.model.Review">
		SELECT 
			`id`
			, `storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
			, `createdAt`
			, `updatedAt`
		FROM 
			`new_review`
		WHERE
			`id` = #{id}
	</select>
	
	<insert id="insertReview" parameterType="map">
	
		INSERT INTO
			`new_review`
		(
			 `storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
			, `createdAt`
			, `updatedAt`
		)
		VALUES
		(
		<!-- @Param 키워드 -->
			#{storeId}
			, #{menu}
			, #{userName}
			, #{point}
			, #{review}
			, now()
			, now()
		)
	
	
	</insert>
	
	<insert id="insertReviewByObject" parameterType="com.nunbub.spring.ex.mybatis.model.Review">
		INSERT INTO
			`new_review`
		(
			 `storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
			, `createdAt`
			, `updatedAt`
		)
		VALUES
		(
		<!-- Review 클래스의 멤버변수 이름 -->
			#{storeId}
			, #{menu}
			, #{userName}
			, #{point}
			, #{review}
			, now()
			, now()
		)
	
	</insert>


</mapper>